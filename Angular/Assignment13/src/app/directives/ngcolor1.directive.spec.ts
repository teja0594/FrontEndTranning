import { Ngcolor1Directive } from './ngcolor1.directive';

describe('Ngcolor1Directive', () => {
  it('should create an instance', () => {
    const directive = new Ngcolor1Directive();
    expect(directive).toBeTruthy();
  });
});
