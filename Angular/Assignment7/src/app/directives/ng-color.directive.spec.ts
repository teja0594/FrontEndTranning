import { NgColorDirective } from './ng-color.directive';

describe('NgColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgColorDirective();
    expect(directive).toBeTruthy();
  });
});
